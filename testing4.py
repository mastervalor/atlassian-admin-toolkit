array = [5,3,4,1]

x = 0
y= len(array) -1

for x =0 in x<array.length:

    for y in y>=x:
    {
        if(array[x]> array[y])
        {
            var temp = array[y];
            array[y] = array[x];
            array[x] =
        }
    }
}